#include <bits/stdc++.h>

using namespace std;

int main() {
    vector<char> v;
    const string numStr = "swiper";
    char c;
    while (cin >> c) {
        v.push_back(c);
    }
    // cin>>string numStr;
    for (char c : v) {
        if ()
    }
    find()
}